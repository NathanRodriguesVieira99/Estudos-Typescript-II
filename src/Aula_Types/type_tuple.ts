

// Type TUPLE


let aluno: [string, number]; // define que vai ter apenas o número de elementos dentro do []

aluno = ['aluno1', 123]
aluno.push('aluno2', 321)
console.log(aluno);


let statusPedido: [string, string, string];

statusPedido = ['em Produção', 'saiu para entrega', 'pedido entregue']
console.log(statusPedido);

