

// Type BOOLEAN

let isAutenticaded: boolean = true;

let codeStatus: number = 1

// Tudo que for diferente de 0 , '' ou undefined será true.
isAutenticaded = Boolean(codeStatus)

console.log(isAutenticaded);

